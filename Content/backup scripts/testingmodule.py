import unreal_engine as ue
import tensorflow as tf

class Load:

    # this is called on game start
    def begin_play(self):
        ue.log('Begin Play tf load test')


    # this is called at every 'tick'    
    # def tick(self, delta_time):
        # do nothing for now
